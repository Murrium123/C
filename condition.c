#include <stdio.h>

int main (void) {
      int grade =50;
      if (grade > 60){printf("You passed! Your grade is %d\n",grade);}
      else{printf("I am sorry buddy, you didn't make it this time. ");}
	return (0);
}
